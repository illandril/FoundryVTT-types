declare global {
  namespace foundry {
    namespace CONST {
      const KEYBINDING_PRECEDENCE: {
        PRIORITY: number
        NORMAL: number
        DEFERRED: number
      };
    }
  }
}

export {};
